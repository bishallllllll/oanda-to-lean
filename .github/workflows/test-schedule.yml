name: test-schedule
on:
  schedule:
    - cron: '*/5 * * * *'

concurrency:
  group: test-sched-${{ inputs.axis || 'none' }}
  cancel-in-progress: false

jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - name: sched-probe
        run: echo "schedule fired at $(date -u +%H:%M:%S)"
